{% docs kpi_hours_per_day %}
**Hours per day (staff)**: Count of hours with presence during local business
window (08:00–20:00 site TZ) divided by number of weekdays in scope. Uses
`FACT_STAFF_PRESENCE_HOURLY` filtered to clean audits.
{% enddocs %}
